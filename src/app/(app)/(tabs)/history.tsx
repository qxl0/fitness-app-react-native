import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function history() {
  return (
    // History component implementation goes here
    <SafeAreaView>
        <Text>History Page</Text>
    </SafeAreaView>
  );
}